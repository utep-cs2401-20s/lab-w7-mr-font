public class SortOfSort {




    public  static  void sortOfSort(int[] unsortedArray){

        int sortedOffsetRight = 1;
        int sortedOffsetLeft = 0;
        int offSet = 2;
        int loopNum = 0;
        int temp = 0;

        //false = left, true = right
        boolean leftOrRight;
        int largestNum = unsortedArray[unsortedArray.length-1];

        for(int i = 0; i < unsortedArray.length-1; ++i){

            if(unsortedArray[i] > largestNum){
                temp = largestNum;

            }
        }
    }

    public int[] sortOfHelper(int[] unsortedArray, boolean leftRight, ){


    }
}
